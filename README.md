# File-Browse-scp

## **リポジトリ概要**
- 名前: `File-Browse-scp`
- 作成日: 2026.8.10

## **ルート構成**
- `.gitignore`
- `README.md`
- `File-ops-main/`

## **`File-ops-main/`**
構成:
- ディレクトリ: `.agents`, `artifacts`, `attached_assets`, `docs`, `file-browser`, `scripts`
- ファイル: `.replit`, `.replitignore`, `.npmrc`, `main.py`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `pyproject.toml`, `replit.md`, `tsconfig.base.json`, `tsconfig.json`, `uv.lock`
- Python(pyproject.toml, uv.lock)
- Node/TypeScript(package.json, pnpm-workspace, tsconfig)
